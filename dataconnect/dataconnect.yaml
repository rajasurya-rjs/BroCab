specVersion: "v1"
serviceId: "brocab"
location: "us-central1"
schema:
  source: "./schema"
  datasource:
    postgresql:
      database: "fdcdb"
      cloudSql:
        instanceId: "brocab-fdc"
      # schemaValidation: "COMPATIBLE"
connectorDirs: ["./connector"]
